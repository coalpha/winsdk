{
   "version": "0.2.0",
   "configurations": [
      {
         "type": "lldb",
         "request": "launch",
         "name": "debug",
         "program": "${workspaceFolder}/bin/winsdk.exe",
         "args": ["--kit:ucrt", "--arch:arm64", "--type:include"],
         "cwd": "${workspaceFolder}"
      }
   ]
}
